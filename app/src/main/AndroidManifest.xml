<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CHKAssetManager">

        <activity android:name=".ui.BulkRegionActivity" />
        <activity android:name=".ui.EditAssetActivity" />
        <activity android:name=".ui.AssetListActivity" />
        <activity android:name=".ui.EditRegionActivity" />
        <activity android:name=".ui.GameDetailActivity" />
        <activity android:name=".ui.EditGameActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
